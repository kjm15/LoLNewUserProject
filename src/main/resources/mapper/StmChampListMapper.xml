<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.projectFinal.dao.StmUserListDao">
<select id="MatchList">
select count(matchId) from UserListT where matchId = #{matchId};
</select>
</mapper>